defmodule ApiHarness.Agent.Tools.ReadDocument do
  @moduledoc "Stub tool: read a document from the user's file metadata (FR-027 placeholder)."

  def run(_input), do: {:ok, "Document reading not yet implemented."}
end
